# 評価

PC向けのアプリケーションを作る場合は、これでいいかも。

Webの技術が使えるので、潰しが効きやすい。

## 実装

`npm create electron-vite@latest`を使いmacで実装するのが無難。

macでは、ホットリロードが一通り動作する。

wslだと、vue側のホットリロードは普通に動作するけど、electron側のホットリロードで消せなくなるwindowが増殖する。
